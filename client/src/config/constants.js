
export const JWT_TOKEN_HEADER = 'X-Token';

export const DEFAULT_PAGE_SIZE = 10;

export const LS_JWT_TOKEN_KEY = `token`;
export const LS_USER_KEY = `user`;


export const ROUTES = {
    LOGIN_LAYOUT: '/',
    REGISTER_LAYOUT: '/register',
    PEST_DETECTION_GALLERY: `/gallery`,
};
